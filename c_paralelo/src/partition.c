#include "partition.h"
#include "matrix.h"
int partition_rows(size_t n, int processes, int *counts, int *displacements) {
    (void)n; (void)processes; (void)counts; (void)displacements;
    return MATRIX_PENDING;
}