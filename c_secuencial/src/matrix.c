#include "matrix.h"
int matrix_multiply(const double *a, const double *b, double *c, size_t n) {
    (void)a; (void)b; (void)c; (void)n;
    /* Deliberately leave output untouched: this is NOT a multiplication. */
    return MATRIX_PENDING;
}